What each import does:

createContext - Creates the context (the electrical grid)
useContext - Lets components access the context (plug into outlets)
useState - Stores the current theme
useEffect - Runs code when component loads (to load saved theme)
useColorScheme - Detects if user's phone is in dark mode
AsyncStorage - Saves data to phone (like localStorage on web)
